# setup.py

from setuptools import find_packages, setup  # Remove convert_path

# Define a dummy convert_path function if necessary
def convert_path(path):
    return path

# Continue with the rest of the setup script